#include <stdio.h>
#include <stdlib.h>
#include <string.h>

enum boolean { NO, YES };
enum months { JAN = 1, FEB, MAR, APR, MAY, JUN, JUL, AUG, SEP, OCT, NOV, DEC };

int main(int argc, char *argv[])
{
    printf("%d\n", FEB);
    return 0;
}
