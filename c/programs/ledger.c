/**
 * Can we just do this by sorting by date? I think we can sort this by date.
 * Dates are a special mix of different int bases.
 *
 * Two arguments, read and write.
 */
#include <stdio.h>


struct LedgerItem {
    int date;
} LedgerItem;


int main(int argc, char *argv[])
{
    return 0;
}
